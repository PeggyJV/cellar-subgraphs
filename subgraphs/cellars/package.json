{
  "name": "cellars",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "graph build",
    "codegen": "graph codegen",
    "create-local": "graph create --node http://localhost:8020 sommelier/cellars",
    "remove-local": "graph remove --node http://localhost:8020 sommelier/cellars",
    "deploy-local": "graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 sommelier/cellars",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.25.0",
    "matchstick-as": "^0.4.0",
    "semver": "^7.3.5",
    "u32": "^1.0.3"
  },
  "devDependencies": {
    "matchstick-as": "^0.4.0"
  }
}
