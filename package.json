{
  "name": "@sommelier/subgraphs",
  "description": "Subgraphs that track Cellars and more on Sommelier Network",
  "private": true,
  "workspaces": [
    "packages/*",
    "subgraphs/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "codegen": "yarn workspaces run codegen",
    "test": "yarn workspaces run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PeggyJV/cellar-subgraphs.git"
  },
  "author": "sandbochs",
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.29.0",
    "@graphprotocol/graph-ts": "^0.26.0",
    "@trivago/prettier-plugin-sort-imports": "^3.2.0",
    "matchstick-as": "^0.3.0",
    "prettier": "^2.6.0"
  }
}
